@using Будівельна_компанія.Domain.Entities
@model IQueryable<Будівельна_компанія.Domain.Entities.ServiceItem>

@if (Model?.Any() == true)
{
    <section>
        <h2>Наші послуги</h2>
        <ul class="small-image-list">
            @foreach (ServiceItem entity in Model)
            {
                <li>
                    <a asp-area="" asp-controller="Services" asp-action="Index" asp-route-id="@entity.Id">
                        <img src="~/images/@entity.TitleImagePath" alt="" class="left" />
                    </a>
                    <h4>
                        @entity.Title
                    </h4>
                    <p>
                        @entity.Subtitle
                    </p>
                </li>
            }
        </ul>
    </section>
}